import SwiftUI

enum TTCFonts {
    static let routeNumber = Font.system(.title2, design: .rounded, weight: .bold)
    static let routeName = Font.system(.body, design: .default, weight: .medium)
    static let caption = Font.system(.caption, design: .default, weight: .regular)
    static let countdown = Font.system(.title, design: .monospaced, weight: .semibold)
    static let badge = Font.system(.caption2, design: .rounded, weight: .semibold)
}
